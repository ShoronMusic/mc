# 荒らし対策・モデレーション — 今後の課題メモ

チャット・選曲・AI API の**悪用・スパム・嫌がらせ**に備えた拡張を、実装状況とあわせて整理する。法務・プライバシーは方針決めのうえで進めること。

---

## 現状（実装済み・緩和策）

| 領域 | 内容 |
|------|------|
| AI チャット | IP 単位の短時間レート制限（`src/lib/chat-ai-rate-limit.ts`、`getChatAiClientIp` は `x-forwarded-for` / `x-real-ip`）。**メモリ保持のみ**で、BAN リストや永続ログではない |
| 質問ガード等 | 別系統のレート制限（例: `src/lib/question-guard-classify-rate-limit.ts`） |
| AI 質問ガード | 不適切な「@」質問の警告・累積後の退場（ログインユーザー中心。免除 UID は `src/lib/ai-question-guard-exempt-user-ids.ts`） |
| ゲスト | 表示名・同期部屋の `clientId`（Ably）で区別。**Supabase `user_id` が付かない**ことが多く、別ブラウザ・別端末では同一人物の追跡は困難 |
| ミドルウェア | OAuth 救済のみ（`src/middleware.ts`）。IP ブロックなし |

---

## 限界・前提（設計のときに読む）

- **IP だけでの特定・BAN**は弱い: CGNAT・共有 Wi‑Fi・VPN・回線変更で、同一 IP に複数人がいる／一人が IP を変えやすい。
- **ゲストに IP を保存する**場合は、利用目的・保存期間・第三者提供の有無を**プライバシーポリシー等に明記**し、必要最小限にすることが前提（個人情報保護法・GDPR 等の論点になりうる）。
- **サーバーレス**ではインメモリのレート制限はインスタンス単位でばらつく。厳密な制限は**エッジ / WAF / 専用プロキシ**との併用が現実的（`chat-ai-rate-limit.ts` コメントにも記載あり）。

---

## 今後の課題（ToDo）

優先度は運用負荷・被害の出方に合わせて調整する。

### 1. アカウント・認証まわり（効果が大きい）

| # | 内容 |
|---|------|
| M1 | **問題のある操作だけログイン必須**（例: チャット送信、特定 API）。ゲストは閲覧のみ、など段階的にも可 |
| M2 | **登録ユーザー向け BAN**: Supabase の `user_id` またはメールで拒否（RLS・API 先頭・カスタムクレーム等の設計要検討） |
| M3 | **管理者用の「アカウント停止」フロー**（理由・期間・解除。監査ログの有無） |

### 2. ネットワーク・インフラ（即効性があることが多い）

| # | 内容 |
|---|------|
| M4 | **ホスティング / CDN の WAF・ボット対策・国別・IP レンジブロック**（Vercel / Cloudflare 等。アプリ外レイヤー） |
| M5 | **既知の攻撃元 IP の一時ブロック**（運用ルール: 誤爆時の解除手順） |

### 3. アプリ内のログ・モデレーション（ゲスト含む）

| # | 内容 |
|---|------|
| M6 | **疑わしいイベントの短期ログ**（例: IP・user_id・room_id・エンドポイント・時刻）。**保存期間を短く**し、目的外利用をしない |
| M7 | **オプションの IP 拒否リスト**（環境変数 or DB）。導入時は `x-forwarded-for` の**信頼できるプロキシ構成**を明示（偽造ヘッダ対策） |
| M8 | **部屋オーナー / モデレーターによるキック・ミュート**（Ably メタデータ・クライアント側強制退室の設計） |
| M9 | **通報フロー**（チャット行単位、通報理由、管理者キュー。既存のサイトフィードバックとの役割分担） |

### 4. プロダクト・UX

| # | 内容 |
|---|------|
| M10 | **新規ゲストのクールダウン**（入室直後は投稿不可 N 秒、など） |
| M11 | **同一部屋内の連投・重複コンテンツ検知**（クライアントまたは軽量サーバールール） |
| M12 | **AI コスト上限**（部屋単位・日次。既存のレート制限と併用） |

### 5. ドキュメント・運用

| # | 内容 |
|---|------|
| M13 | **インシデント対応手順**（誰が BAN するか、証跡の残し方、ユーザーへの連絡） |
| M14 | **利用規約・ポリシーとの整合**（禁止事項、措置内容、ログの説明） |

---

## 参考コードパス

- `src/lib/chat-ai-rate-limit.ts` — チャット AI 用 IP 取得・レート制限
- `src/lib/question-guard-classify-rate-limit.ts` — 質問ガード分類まわり
- `src/middleware.ts` — 現状は OAuth のみ
- `AGENTS.md` — AI ガード・ポリシー関連の索引

---

## 関連ドキュメント

- `docs/feedback-and-ai-improvement-todo.md` — フィードバック・品質改善（ユーザー特定は優先度低として別枠）
- `docs/supabase-setup.md` — DB・RLS の一般的な注意
